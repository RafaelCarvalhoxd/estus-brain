# Só para rodar na máquina: `docker compose` carrega este arquivo sozinho.
# O deploy usa apenas o docker-compose.yml.
services:
  postgres:
    ports:
      - "127.0.0.1:5434:5432"
  backend:
    ports:
      - "127.0.0.1:8080:8080"
  frontend:
    ports:
      - "127.0.0.1:3000:3000"
